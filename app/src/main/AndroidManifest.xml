<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EmprendeIstg"
        tools:targetApi="31">

        <activity
            android:name=".ui.activitys.Invitado"
            android:exported="false" />

        <!-- MainActivity: receptor de deep links (desactivado temporalmente) -->
        <activity
            android:name=".MainActivity"
            android:exported="false">
            <!--
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="app" android:host="profile" />
            </intent-filter>
            -->
        </activity>

        <activity
            android:name=".InicioActivity"
            android:exported="true"
            android:label="@string/title_activity_inicio"
            android:theme="@style/Theme.EmprendeIstg.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- esquema personalizado: app://profile/{userId} -->
                <data android:scheme="app" android:host="profile" />
            </intent-filter>
        </activity>
        <!-- PantallaInicialActivity: LAUNCHER -->
        <activity
            android:name=".ui.activitys.PantallaInicialActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- NOTA: NO declarar Fragments como <activity> en el manifest. -->
    </application>

</manifest>